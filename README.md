# JobGuard Flask

Учебный Flask-проект сервиса JobGuard.

## Локальный запуск в VS Code на Windows

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py